# Contributing to WindTool

感谢你参与 WindTool。这个项目直接接触宿主进程内存，因此兼容性与失败保护优先于功能数量。

项目作者与维护者：克里斯提亚娜。

## 开发流程

1. 从独立分支开始修改。
2. 保持对 MMD 9.31 x64 的版本与线程校验。
3. 为风场数学、JSON 兼容、目标选择或宿主契约变更补充测试。
4. 运行 `pwsh -File .\scripts\Build-WindTool.ps1`。
5. 在 Pull Request 中说明行为变化、测试结果和人工 MMD 验证步骤。

## 提交边界

- 不要提交 MMD、MME 或其他第三方闭源二进制。
- 不要提交模型、动作、PMM、用户 JSON、日志、转储或个人绝对路径。
- 不要移除宿主 SHA-256 校验来扩大兼容范围。
- 新宿主版本应使用独立布局描述、测试夹具和明确的兼容性记录。
- UI 文本默认使用中文；标识符与代码注释使用清晰的英文。

## 代码风格

- C++17，改动尽量保持局部和可审查。
- 所有内存写入必须有读取/写入范围检查及失败回滚路径。
- 不在 MME 回调中执行阻塞 I/O 或长时间持锁。
- JSON 格式变更必须继续读取旧版轨道文件。
